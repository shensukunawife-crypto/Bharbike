<div class="stats-grid">
  <div class="stat-card" style="display: flex; align-items: center; gap: 16px;">
    <% if (user.selfie_url) { %>
      <img src="<%= user.selfie_url %>" alt="User Selfie" style="width: 50px; height: 50px; border-radius: 50%; object-fit: cover; border: 2px solid var(--accent);">
    <% } else { %>
      <div style="width: 50px; height: 50px; border-radius: 50%; background: #e5e7eb; display: flex; align-items: center; justify-content: center; font-size: 24px;">👤</div>
    <% } %>
    <div>
      <div class="stat-head" style="margin-bottom: 4px;"><span class="stat-label">User</span></div>
      <strong class="stat-value"><%= user.full_name || "N/A" %></strong>
    </div>
  </div>
  <div class="stat-card"><div class="stat-head"><span class="stat-icon">🧾</span><span class="stat-label">Total Orders</span></div><strong class="stat-value"><%= user.totalOrders %></strong></div>
  <div class="stat-card"><div class="stat-head"><span class="stat-icon">💳</span><span class="stat-label">Total Spent</span></div><strong class="stat-value revenue-positive">₹<%= Number(user.totalSpent || 0).toFixed(2) %></strong></div>
  <div class="stat-card"><div class="stat-head"><span class="stat-icon">💰</span><span class="stat-label">Wallet Balance</span></div><strong class="stat-value" style="color: var(--accent);">₹<%= Number(user.walletBalance || 0).toFixed(2) %></strong></div>
</div>

<div class="analytics-grid">
  <div class="chart-card">
    <div style="display: flex; justify-content: space-between; align-items: center;">
      <h3 style="margin: 0;">User Details</h3>
      <button onclick="document.getElementById('edit-profile-modal').classList.add('show')" class="btn btn-sm btn-ghost" style="font-size: 12px; padding: 4px 8px;">Edit Profile</button>
    </div>
    <div class="summary-list" style="display: flex; flex-direction: column; gap: 12px; margin-top: 16px;">
      <div style="display: flex; justify-content: space-between; padding-bottom: 8px; border-bottom: 1px solid #e5e7eb;">
        <span style="color: #6b7280; font-weight: 500;">User ID</span>
        <strong style="text-align: right; word-break: break-all; max-width: 60%; cursor: help;" title="<%= user.id %>">
          <%= String(user.id).substring(0, 8) %>...<%= String(user.id).slice(-4) %>
        </strong>
      </div>
      <div style="display: flex; justify-content: space-between; padding-bottom: 8px; border-bottom: 1px solid #e5e7eb;">
        <span style="color: #6b7280; font-weight: 500;">Full Name</span>
        <strong style="text-align: right;"><%= user.full_name || "N/A" %></strong>
      </div>
      <div style="display: flex; justify-content: space-between; padding-bottom: 8px; border-bottom: 1px solid #e5e7eb;">
        <span style="color: #6b7280; font-weight: 500;">Phone</span>
        <strong style="text-align: right;"><%= user.phone || "N/A" %></strong>
      </div>
      <div style="display: flex; justify-content: space-between; padding-bottom: 8px; border-bottom: 1px solid #e5e7eb;">
        <span style="color: #6b7280; font-weight: 500;">Email</span>
        <strong style="text-align: right; word-break: break-all; max-width: 60%;"><%= user.email || "N/A" %></strong>
      </div>
      <div style="display: flex; justify-content: space-between; padding-bottom: 8px; border-bottom: 1px solid #e5e7eb;">
        <span style="color: #6b7280; font-weight: 500;">Address</span>
        <strong style="text-align: right; word-break: break-word; max-width: 60%;"><%= user.address %> 
          <% if (user.address_verified) { %><span style="color: var(--success); font-size: 11px;">(Verified)</span><% } %>
        </strong>
      </div>
      <div style="display: flex; justify-content: space-between; padding-bottom: 8px; border-bottom: 1px solid #e5e7eb;">
        <span style="color: #6b7280; font-weight: 500;">Emergency Contact</span>
        <strong style="text-align: right;"><%= user.emergency_contact_name %> <br> <span style="font-size: 12px; color: var(--text-muted);"><%= user.emergency_contact_phone %></span></strong>
      </div>
      <div style="display: flex; justify-content: space-between; padding-bottom: 8px; border-bottom: 1px solid #e5e7eb;">
        <span style="color: #6b7280; font-weight: 500;">Location</span>
        <strong style="text-align: right;"><%= user.location || "N/A" %></strong>
      </div>
      <div style="display: flex; justify-content: space-between; align-items: center; padding-bottom: 8px; border-bottom: 1px solid #e5e7eb;">
        <span style="color: #6b7280; font-weight: 500;">Assigned Bike</span>
        <div style="display: flex; align-items: center; gap: 8px;">
          <strong style="text-align: right; color: var(--accent);"><%= user.assignedBike %></strong>
          <button onclick="document.getElementById('change-bike-modal').classList.add('show')" class="btn btn-sm" style="font-size: 11px; padding: 2px 6px; background: #e5e7eb; color: #333;">Change</button>
        </div>
      </div>
      <div style="display: flex; justify-content: space-between; padding-bottom: 8px;">
        <span style="color: #6b7280; font-weight: 500;">Joined Date</span>
        <strong style="text-align: right;"><%= String(user.joinedDate).slice(0, 10) %></strong>
      </div>
    </div>
  </div>

  <div class="chart-card">
    <h3>User Activity</h3>
    <div class="summary-list" style="display: flex; flex-direction: column; gap: 12px; margin-top: 16px;">
      <div style="display: flex; justify-content: space-between; padding-bottom: 8px; border-bottom: 1px solid #e5e7eb;">
        <span style="color: #6b7280; font-weight: 500;">Last Login</span>
        <strong style="text-align: right;"><%= String(user.lastLogin).slice(0, 16).replace("T", " ") %></strong>
      </div>
      <div style="display: flex; justify-content: space-between; padding-bottom: 8px; border-bottom: 1px solid #e5e7eb;">
        <span style="color: #6b7280; font-weight: 500;">Last Order ID</span>
        <strong style="text-align: right; word-break: break-all; max-width: 60%; cursor: help;" title="<%= user.lastOrder %>">
          <%= user.lastOrder !== "N/A" ? String(user.lastOrder).substring(0, 8) + '...' + String(user.lastOrder).slice(-4) : "N/A" %>
        </strong>
      </div>
      <div style="display: flex; justify-content: space-between; padding-bottom: 8px;">
        <span style="color: #6b7280; font-weight: 500;">Last Active</span>
        <strong style="text-align: right;"><%= user.lastActive %></strong>
      </div>
    </div>
  </div>
</div>

<div class="chart-card" style="margin-top: 24px;">
  <h3>KYC & Documents</h3>
  <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 16px; margin-top: 16px;">
    
    <!-- Aadhaar Front -->
    <div style="border: 1px solid var(--border-light); padding: 12px; border-radius: 8px;">
      <h4 style="margin: 0 0 8px 0; font-size: 13px; color: var(--text-muted);">Aadhaar Front</h4>
      <% if (user.aadhaar_front_url) { %>
        <a href="<%= user.aadhaar_front_url %>" target="_blank">
          <img src="<%= user.aadhaar_front_url %>" alt="Aadhaar Front" style="width: 100%; height: 120px; object-fit: contain; border-radius: 4px; background: #f3f4f6;">
        </a>
      <% } else { %>
        <div style="height: 120px; display: flex; align-items: center; justify-content: center; background: #f3f4f6; border-radius: 4px; color: #9ca3af; font-size: 12px;">Not Uploaded</div>
      <% } %>
      <form class="upload-doc-form" data-type="aadhaar_front" style="margin-top: 12px; display: flex; flex-direction: column; gap: 8px;">
        <input type="file" accept="image/*,application/pdf" required style="font-size: 11px;">
        <button type="submit" class="btn btn-ghost" style="padding: 4px 8px; font-size: 12px;">Upload / Replace</button>
      </form>
    </div>

    <!-- Aadhaar Back -->
    <div style="border: 1px solid var(--border-light); padding: 12px; border-radius: 8px;">
      <h4 style="margin: 0 0 8px 0; font-size: 13px; color: var(--text-muted);">Aadhaar Back</h4>
      <% if (user.aadhaar_back_url) { %>
        <a href="<%= user.aadhaar_back_url %>" target="_blank">
          <img src="<%= user.aadhaar_back_url %>" alt="Aadhaar Back" style="width: 100%; height: 120px; object-fit: contain; border-radius: 4px; background: #f3f4f6;">
        </a>
      <% } else { %>
        <div style="height: 120px; display: flex; align-items: center; justify-content: center; background: #f3f4f6; border-radius: 4px; color: #9ca3af; font-size: 12px;">Not Uploaded</div>
      <% } %>
      <form class="upload-doc-form" data-type="aadhaar_back" style="margin-top: 12px; display: flex; flex-direction: column; gap: 8px;">
        <input type="file" accept="image/*,application/pdf" required style="font-size: 11px;">
        <button type="submit" class="btn btn-ghost" style="padding: 4px 8px; font-size: 12px;">Upload / Replace</button>
      </form>
    </div>

    <!-- Driving License -->
    <div style="border: 1px solid var(--border-light); padding: 12px; border-radius: 8px;">
      <h4 style="margin: 0 0 8px 0; font-size: 13px; color: var(--text-muted);">Driving License</h4>
      <% if (user.driving_license_url) { %>
        <a href="<%= user.driving_license_url %>" target="_blank">
          <img src="<%= user.driving_license_url %>" alt="Driving License" style="width: 100%; height: 120px; object-fit: contain; border-radius: 4px; background: #f3f4f6;">
        </a>
      <% } else { %>
        <div style="height: 120px; display: flex; align-items: center; justify-content: center; background: #f3f4f6; border-radius: 4px; color: #9ca3af; font-size: 12px;">Not Uploaded</div>
      <% } %>
      <form class="upload-doc-form" data-type="driving_license" style="margin-top: 12px; display: flex; flex-direction: column; gap: 8px;">
        <input type="file" accept="image/*,application/pdf" required style="font-size: 11px;">
        <button type="submit" class="btn btn-ghost" style="padding: 4px 8px; font-size: 12px;">Upload / Replace</button>
      </form>
    </div>

    <!-- PAN Card -->
    <div style="border: 1px solid var(--border-light); padding: 12px; border-radius: 8px;">
      <h4 style="margin: 0 0 8px 0; font-size: 13px; color: var(--text-muted);">PAN Card</h4>
      <% if (user.pan_card_url) { %>
        <a href="<%= user.pan_card_url %>" target="_blank">
          <img src="<%= user.pan_card_url %>" alt="PAN Card" style="width: 100%; height: 120px; object-fit: contain; border-radius: 4px; background: #f3f4f6;">
        </a>
      <% } else { %>
        <div style="height: 120px; display: flex; align-items: center; justify-content: center; background: #f3f4f6; border-radius: 4px; color: #9ca3af; font-size: 12px;">Not Uploaded</div>
      <% } %>
      <form class="upload-doc-form" data-type="pan_card" style="margin-top: 12px; display: flex; flex-direction: column; gap: 8px;">
        <input type="file" accept="image/*,application/pdf" required style="font-size: 11px;">
        <button type="submit" class="btn btn-ghost" style="padding: 4px 8px; font-size: 12px;">Upload / Replace</button>
      </form>
    </div>

    <!-- Electricity Bill -->
    <div style="border: 1px solid var(--border-light); padding: 12px; border-radius: 8px;">
      <h4 style="margin: 0 0 8px 0; font-size: 13px; color: var(--text-muted);">Electricity Bill</h4>
      <% if (user.electricity_bill_url) { %>
        <a href="<%= user.electricity_bill_url %>" target="_blank">
          <img src="<%= user.electricity_bill_url %>" alt="Electricity Bill" style="width: 100%; height: 120px; object-fit: contain; border-radius: 4px; background: #f3f4f6;">
        </a>
      <% } else { %>
        <div style="height: 120px; display: flex; align-items: center; justify-content: center; background: #f3f4f6; border-radius: 4px; color: #9ca3af; font-size: 12px;">Not Uploaded</div>
      <% } %>
      <form class="upload-doc-form" data-type="electricity_bill" style="margin-top: 12px; display: flex; flex-direction: column; gap: 8px;">
        <input type="file" accept="image/*,application/pdf" required style="font-size: 11px;">
        <button type="submit" class="btn btn-ghost" style="padding: 4px 8px; font-size: 12px;">Upload / Replace</button>
      </form>
    </div>

  </div>
</div>

<div class="chart-card">
  <h3>Order History</h3>
  <div class="table-wrap">
    <table class="table">
      <thead>
        <tr>
          <th>Order ID</th>
          <th>Pickup</th>
          <th>Drop</th>
          <th>Status</th>
          <th>Amount</th>
        </tr>
      </thead>
      <tbody>
        <% if (!orderHistory.length) { %>
        <tr><td colspan="5" class="empty">No orders found</td></tr>
        <% } %>
        <% orderHistory.forEach((order) => { %>
        <tr>
          <td title="<%= order.id %>" style="cursor: help;">
            <%= String(order.id).substring(0, 8) %>...<%= String(order.id).slice(-4) %>
          </td>
          <td><%= order.pickup_location %></td>
          <td><%= order.drop_location %></td>
          <td><span class="badge"><%= order.status %></span></td>
          <td class="revenue-positive">₹<%= Number(order.amount || 0).toFixed(2) %></td>
        </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</div>

<!-- Edit Profile Modal -->
<div class="modal" id="edit-profile-modal">
  <div class="modal-card">
    <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom: 16px;">
      <h3 style="margin: 0;">Edit User Profile</h3>
      <button type="button" style="background:none; border:none; font-size:24px; cursor:pointer; color:#6b7280;" onclick="document.getElementById('edit-profile-modal').classList.remove('show')">&times;</button>
    </div>
    <form class="admin-form ajax-form" data-url="/admin/users/<%= user.id %>/edit">
      <div style="margin-bottom: 12px;">
        <label style="display:block; font-size:12px; color:#6b7280; margin-bottom:4px;">Full Name</label>
        <input name="full_name" value="<%= user.full_name %>" required />
      </div>
      <div style="margin-bottom: 12px;">
        <label style="display:block; font-size:12px; color:#6b7280; margin-bottom:4px;">Phone</label>
        <input name="phone" value="<%= user.phone %>" required />
      </div>
      <div style="margin-bottom: 12px;">
        <label style="display:block; font-size:12px; color:#6b7280; margin-bottom:4px;">Email</label>
        <input name="email" value="<%= user.email %>" />
      </div>
      <div style="margin-bottom: 12px;">
        <label style="display:block; font-size:12px; color:#6b7280; margin-bottom:4px;">Address</label>
        <input name="address" value="<%= user.address === 'N/A' ? '' : user.address %>" />
      </div>
      <div style="margin-bottom: 12px;">
        <label style="display:block; font-size:12px; color:#6b7280; margin-bottom:4px;">Emergency Contact Name</label>
        <input name="emergency_contact_name" value="<%= user.emergency_contact_name === 'N/A' ? '' : user.emergency_contact_name %>" />
      </div>
      <div style="margin-bottom: 16px;">
        <label style="display:block; font-size:12px; color:#6b7280; margin-bottom:4px;">Emergency Contact Phone</label>
        <input name="emergency_contact_phone" value="<%= user.emergency_contact_phone === 'N/A' ? '' : user.emergency_contact_phone %>" />
      </div>
      <button type="submit" class="btn btn-gold glow" style="width: 100%;">Save Changes</button>
    </form>
  </div>
</div>

<!-- Change Bike Modal -->
<div class="modal" id="change-bike-modal">
  <div class="modal-card">
    <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom: 16px;">
      <h3 style="margin: 0;">Assign / Change Bike</h3>
      <button type="button" style="background:none; border:none; font-size:24px; cursor:pointer; color:#6b7280;" onclick="document.getElementById('change-bike-modal').classList.remove('show')">&times;</button>
    </div>
    <form class="admin-form ajax-form" data-url="/admin/users/<%= user.id %>/assign-bike">
      <div style="margin-bottom: 16px;">
        <label style="display:block; font-size:12px; color:#6b7280; margin-bottom:4px;">Bike Code</label>
        <input name="bike_code" placeholder="e.g. BB001 (Leave empty to unassign)" value="<%= user.assignedBike === 'None' ? '' : user.assignedBike %>" />
      </div>
      <button type="submit" class="btn btn-gold glow" style="width: 100%;">Assign Bike</button>
    </form>
  </div>
</div>
<script>
  document.querySelectorAll('.upload-doc-form').forEach(form => {
    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      const fileInput = form.querySelector('input[type="file"]');
      const docType = form.getAttribute('data-type');
      if (!fileInput.files[0]) return;
      
      const btn = form.querySelector('button');
      const originalText = btn.innerText;
      btn.innerText = 'Uploading...';
      btn.disabled = true;

      const formData = new FormData();
      formData.append('docType', docType);
      formData.append('file', fileInput.files[0]);

      try {
        const res = await fetch(/admin/users/<%= user.id %>/upload-doc, {
          method: 'POST',
          body: formData
        });
        const data = await res.json();
        if (data.success) {
          alert('Document uploaded successfully!');
          window.location.reload();
        } else {
          alert(data.message || 'Upload failed');
        }
      } catch (err) {
        alert('Upload failed: ' + err.message);
      } finally {
        btn.innerText = originalText;
        btn.disabled = false;
      }
    });
  });
</script>
